import 'dart:io';

import 'package:private_sync/local_directory.dart';
import 'package:private_sync/remote_directory.dart';

class Sync {
  void localToRemote(
      LocalDirectory localDirectory, RemoteDirectory remoteDirectory) {
    print('Syncing local to remote');
  }
}
